export const BASE_URL = import.meta.env.VITE_BASE_URL;

/** Production site URL for canonical links and Open Graph. Set VITE_APP_URL in .env for build. */
export const SITE_URL = import.meta.env.VITE_APP_URL || '';
